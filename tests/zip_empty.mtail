gauge upstream_rt by server
counter zip_empty_ran

# $responses is never created → zip short-circuits (zero iterations).
/(?P<sv>[^ ]+)/ {
  $servers = split(",", $sv)
  zip($servers, $responses) as ($server, $rtv) {
    upstream_rt[$server] = $rtv
    zip_empty_ran++
  }
}
